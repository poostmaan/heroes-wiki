import { createContext } from "react";

// * Puedes especificar con TypeScript que lucira el context
// export const AuthContext = createContext({} as any);
export const AuthContext = createContext({});